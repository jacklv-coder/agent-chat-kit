# Markdown

Markdown is parsed off the main actor into an immutable package-owned render document. Raw HTML is
never executed. Incomplete streaming input must remain displayable as plain text. Code and tables
scroll horizontally inside their own views; the conversation itself never scrolls horizontally.
